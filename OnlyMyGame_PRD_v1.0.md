# OnlyMyGame PRD v1.0 — 런타임 AI 규칙 생성 SRPG

## 1. 제품 목표

OnlyMyGame은 절차 생성 핵사곤 월드에서 채집·수렵·생산·건설·전투·외교를 수행하는 1인용 생존 SRPG다. 핵심 차별점은 매 턴 종료 시 AI가 지금까지의 행동 통계, 세력 관계, 자원, 전황, 다음 턴 행운을 분석해 새로운 게임 규칙과 승리 조건을 생성한다는 점이다.

- 플랫폼: Unity `6000.5.6f1`, PC 데스크톱 브라우저 Web 빌드
- 첫 공개 버전: 30–45분, 20–30턴 버티컬 슬라이스
- 시점: 3인칭 쿼터뷰
- 언어: 한국어
- 조작: 키보드·마우스
- 아트: 3D 월드는 `Assets/KayKit` 에셋만 사용
- 톤: 가족 친화적인 코믹 전투와 과장된 피격 표현
- 핵심 성공 조건:
  - 한 판 동안 AI가 매 턴 1–3개의 의미 있는 규칙 변화를 생성한다.
  - 정복·회유·거래·고용은 고정 엔딩 목록이 아니라 AI가 사용할 수 있는 사례에 불과하다.
  - 생성된 규칙은 C# 컴파일이나 임의 코드 실행 없이 다음 턴에 적용된다.
  - 플레이어가 규칙 변화의 원인, 효과, 영향을 받는 대상을 이해하고 대응할 수 있다.

## 2. 플레이 경험과 게임 시스템

### 핵심 게임 루프

1. 절차 생성된 217칸 규모의 핵사곤 맵에서 세력과 자원을 탐색한다.
2. 하나의 통합 SP 풀을 사용해 모든 아군 유닛과 거점을 지휘한다.
3. 채집·수렵·채광으로 식량, 목재, 석재, 철, 화폐를 확보한다.
4. 본부, 창고, 작업장, 감시탑, 시장, 병영을 건설하거나 업그레이드한다.
5. 전투, 거래, 설득, 고용, 점령 등으로 다른 세력과 관계를 형성한다.
6. 모든 세력의 명령을 동시에 해결한다.
7. AI가 종료된 턴의 결과를 분석해 다음 턴의 규칙과 승리 조건을 생성한다.
8. 생성된 세계에 적응하며 AI가 만든 승리 조건을 달성한다.

### 맵과 세력

- 맵은 반지름 8의 시드 기반 핵사곤으로 생성하며 같은 시드와 규칙 입력은 같은 결과를 만든다.
- 최소 구성은 플레이어, 스켈레톤 적대 세력, 중립 AI 세력이다.
- 지형, 거점, 자원 밀도와 유닛 배치는 시드로 생성되고 이후 AI 규칙이 변경할 수 있다.
- 미탐색 지역은 가리고, 탐색한 지형은 유지하되 시야 밖 유닛은 숨긴다.
- AI는 기존 캐릭터·건물·소품 카탈로그를 이용해 세력 생성, 분열, 합병, 충성도 전환을 규칙으로 만들 수 있다.

### 동시 계획 턴

- 각 세력은 기본 10 SP를 공유하며 규칙과 거점 효과로 증감할 수 있다.
- 이동 1, 채집·사냥·공격·거래·설득·고용 2, 건설·업그레이드 3 SP를 기본값으로 사용한다.
- 플레이어는 SP 범위에서 명령을 예약한 뒤 턴을 확정하며 남은 SP는 소멸한다.
- 적과 중립 AI는 턴 시작 상태만 보고 계획하므로 플레이어가 예약한 명령을 미리 알 수 없다.
- 해결 순서는 `턴 시작 효과 → 이동·충돌 → 거래·외교 → 전투 → 채집·건설 → 지속 효과 → 승패 판정`으로 고정한다.
- 이동 충돌과 확률 판정은 속도, 다음 턴 행운, 시드 기반 난수로 결정하며 명령 전 예상 범위를 표시한다.
- 생성형 AI는 개별 유닛 전술을 조작하지 않고 세계 규칙 생성에만 사용한다.

### 행운과 승패

- 행운은 실시간 1분이 아니라 매 턴 새로 생성한다.
- 턴 N 해결 후 턴 N+1 행운을 결정하고, 해당 값과 월드 상태를 AI 규칙 생성 입력에 포함한다.
- 행운은 HUD의 아이콘·숫자와 월드 조명, 표지판, 캐릭터 동작으로 표현한다.
- 고정 패배 조건은 지휘 본부가 파괴되고 복구 가능한 아군도 없는 상태다.
- AI는 패배 판정을 새로 만들거나 즉시 패배를 선언할 수 없다.
- AI 승리 조건은 계속 추가·교체할 수 있지만:
  - 최소 3턴 동안 유지한다.
  - 교체 1턴 전에 예고한다.
  - 항상 달성 가능한 조건을 최소 하나 유지한다.
  - 자원, 관계, 점령, 처치 등 누적 통계는 교체 후에도 재평가한다.
  - 새 조건은 한 턴 이상 공개된 뒤 턴 종료 시점부터 달성 가능하다.

### UI·연출

- 상시 HUD는 SP, 자원, 행운, 세력 관계, 현재 목표 진행을 아이콘과 숫자로 표시한다.
- 새 규칙은 영향받는 타일·유닛·거점에 표지판, 색, 소품, 애니메이션으로 먼저 표현한다.
- 새로운 규칙의 복잡한 의미를 숨기지 않도록 한국어 규칙 장부를 제공한다.
- 규칙 장부에는 `발생 원인 → 새 조건 → 효과 → 지속 시간 → 승리 조건 영향`을 표시한다.
- AI가 논리 자원이나 새 상태를 만들면 범용 토큰 아이콘, 색상, 한국어 이름을 사용한다. 새 3D 에셋은 생성하지 않는다.

## 3. 런타임 AI 규칙 시스템

### 범용 규칙 VM

AI는 기존 규칙 템플릿을 선택하지 않고, 범용 선언형 AST를 작성한다. VM은 다음 요소를 조합해 기존에 없던 행동, 상태, 연쇄 규칙과 승리 조건을 표현한다.

- 상태: 런, 턴, 세력, 유닛, 건물, 타일 범위의 숫자·불리언·집합·카운터
- 트리거: 턴 시작·종료, 이동, 공격, 처치, 채집, 건설, 거래, 관계 변화, 타일 진입
- 조건: 논리식, 산술식, 거리, 소유권, 태그, 누적 통계, 최근 행동 비율
- 효과: 자원·SP·관계 변경, 상태 부여, 세력 전환, 유닛 생성, 액션 잠금 해제, 예약 이벤트
- 동적 액션: 대상 선택식, 비용, 조건, 효과, 재사용 대기시간으로 새 명령 정의
- 동적 목표: 단계, 진행식, 완료식, 유지 기간으로 승리 계약 정의
- 표현: 한국어 이름·설명, HUD 아이콘, 월드 큐, 영향 대상

금지 항목은 반복문, 재귀, 파일·네트워크 접근, 리플렉션, C#·JavaScript·셰이더 실행, 임의 Unity API 호출이다. AI가 새로운 그래픽·물리·입력 방식을 발명할 수는 없지만, 제공된 범용 이벤트와 효과를 이용한 새로운 게임 의미는 만들 수 있다.

### 데이터 계약

- `GameSnapshotV1`: 런 ID, 턴, 시드, 행운, 맵 변화, 엔티티, 세력, 자원, 누적 행동 통계, 활성 규칙, 기능 카탈로그 해시
- `RuleSetV1`: 스키마 버전, 요청 ID, 적용 턴, 한국어 설명, 규칙 변경 목록, 승리 계약, 월드 표현 지시
- `RuleNodeV1`: 상태 정의, 트리거, 조건 AST, 효과 목록, 우선순위, 지속 시간
- `DynamicActionV1`: 대상 선택식, SP·자원 비용, 실행 조건, 효과, 쿨다운
- `VictoryContractV1`: 진행식, 완료식, 최소 유지 턴, 교체 예고, 관련 월드 큐
- `RuleValidationResult`: 오류 코드, AST 위치, 수정 가능한 진단 메시지

공통 계약과 VM은 Unity 의존성이 없는 C# 라이브러리로 만들고 Unity 클라이언트와 NAS 서비스가 같은 검증 코드를 사용한다.

### 생성·검증 흐름

1. 턴 결과를 확정하고 자동 저장 후보 상태를 만든다.
2. 다음 턴 행운과 압축된 `GameSnapshotV1`을 NAS에 전송한다.
3. NAS가 OpenAI Responses API의 `gpt-5.6-luna`, Structured Outputs, `reasoning.effort: medium`으로 `RuleSetV1`을 요청한다.
4. 서버가 JSON 스키마, 타입, 실행 예산, 공정성, 목표 도달성을 검증한다.
5. 클라이언트가 같은 규칙을 독립적으로 다시 검증한다.
6. 성공한 규칙과 턴 상태를 함께 저장하고 다음 턴에 적용한다.
7. 실패하면 진단 정보를 포함해 AI에 한 번 자동 수정 요청한다.
8. 두 번째 실패 또는 20초 타임아웃 시 게임을 차단하고 `재시도` 또는 `저장 후 나가기`를 제공한다. 로컬 대체 규칙은 사용하지 않는다.

`gpt-5.6-luna`는 Responses API와 Structured Outputs를 지원하는 현재 유효 모델 ID다. [OpenAI 공식 모델 문서](https://developers.openai.com/api/docs/models/gpt-5.6-luna)

### 검증 한계

- 한 턴에 1–3개 규칙을 추가·수정·종료한다.
- 일반 활성 규칙 최대 12개, 승리 계약 최대 3개로 제한한다.
- 이벤트당 AST 노드 256개, 연쇄 깊이 4, 턴당 생성 엔티티 4개로 제한한다.
- 반복 실행, 음수 자원, 존재하지 않는 대상, 실행 불가능한 액션을 거부한다.
- 어떤 세력도 최대 SP가 3 미만이 되거나 모든 행동을 잃을 수 없다.
- 본부 즉시 제거, 고정 패배 우회, 공개되지 않은 규칙, 즉시 승리는 거부한다.
- 승리 조건은 의존성 검사와 6턴 제한 시뮬레이션을 통과해야 한다.
- 모든 규칙 상태 변수는 런 종료까지 보존해 목표 교체 시 기존 성과를 다시 평가한다.

## 4. 서비스·저장·배포

### NAS Docker 서비스

- ASP.NET Core 8 API와 SQLite 영속 볼륨으로 구성한다.
- 공개 엔드포인트:
  - `POST /v1/sessions`: 익명 단기 세션 토큰 발급
  - `POST /v1/rules/generate`: 턴 스냅샷을 받아 검증된 규칙 반환
  - `GET /health`: 모델 설정, DB, 서비스 상태 확인
- OpenAI 키는 NAS Docker secret 또는 환경 변수에만 저장하며 Unity와 GitHub Pages에 포함하지 않는다.
- GitHub Pages 도메인만 CORS 허용하고 TLS, 요청 크기 제한, idempotency key를 적용한다.
- 기본 제한은 IP당 하루 60회 AI 생성, 동시 세션 2개이며 NAS 환경 변수로 조정한다.
- IP는 일일 salt로 해시하고 요청 원문은 저장하지 않는다. 오류·지연·토큰·검증 결과는 30일 보존한다.

Unity Web은 다른 출처의 NAS API 호출에 `UnityWebRequest`와 서버 측 CORS가 필요하다. [Unity 웹 네트워킹 문서](https://docs.unity3d.com/kr/current/Manual/webgl-networking.html)

### 저장

- 브라우저 IndexedDB에 단일 자동 저장 슬롯을 사용한다.
- 매 턴 유효한 AI 규칙 수신 직후 원자적으로 저장한다.
- 저장 항목은 시드, 맵, 엔티티, 누적 통계, 전체 규칙 상태, 대기 중 규칙, 행운, AI 요청 ID와 스키마 버전이다.
- 체크섬, 임시 저장 후 교체, 이전 세대 백업 1개를 적용한다.
- AI 장애 중에도 마지막 해결 턴과 재시도 요청을 저장할 수 있어야 한다.
- 동일한 시드·명령·규칙 응답으로 결정론적 리플레이가 가능해야 한다.

### GitHub CI/CD

- 기능은 브랜치와 PR 단위로 개발하고 완료 시 반드시 원격 저장소에 push한다.
- self-hosted Windows Runner에 Unity `6000.5.6f1`을 고정한다.
- 모든 push에서 컴파일, EditMode, PlayMode, 결정론 테스트와 Web 빌드를 수행한다.
- PR 빌드는 GitHub Pages의 `/previews/pr-{번호}/`에 배포한다.
- `main` 병합 빌드는 Pages 루트에 자동 배포한다.
- NAS 서비스는 다중 아키텍처 Docker 이미지로 GHCR에 게시하고 별도 배포 작업 후 `/health`를 확인한다.
- GitHub Pages에서 사용자 지정 응답 헤더에 의존하지 않도록 첫 버전은 네이티브 C/C++ 멀티스레딩을 끄고 압축 해제 폴백을 사용한다. Unity는 압축 Web 빌드의 서버 헤더와 fallback 설정을 별도로 요구한다. [Unity Web 배포 문서](https://docs.unity3d.com/kr/current/Manual/webgl-deploying.html)

## 5. 구현 단계와 검증

### 구현 단계

1. 프로젝트 기반
   - Unity 프로젝트, Kay 에셋 카탈로그, asmdef, 테스트 구조, Git LFS, GitHub Runner와 Pages 프리뷰를 구축한다.
2. 결정론적 SRPG
   - 핵사곤 생성, 시야, 통합 SP, 동시 계획·해결, 전투와 기본 세력 AI를 완성한다.
3. 생활·성장 루프
   - 채집, 수렵, 생산, 여섯 건물, 업그레이드, 거래·설득·고용을 데이터 기반으로 구현한다.
4. 규칙 VM
   - AST, 동적 상태·액션·목표, 이벤트 버스, 검증기, 실행 예산, 규칙 장부를 구현한다.
5. AI 서비스
   - NAS API, OpenAI 구조화 출력, 자동 수정 재시도, 제한·로그·오류 화면을 연결한다.
6. 버티컬 슬라이스
   - 20–30턴 밸런스, 자동 저장·복원, 월드 연출, 최적화, 공개 Pages 배포를 완료한다.

각 단계는 독립적인 PR과 플레이 가능한 프리뷰 URL을 제공하며, 테스트·Web 빌드·사용자 확인이 끝나야 다음 단계로 넘어간다.

### 필수 테스트와 출시 기준

- 동일 시드·명령·규칙으로 100회 실행했을 때 상태 해시가 동일하다.
- 이동 충돌, 동시 처치, 거래와 공격 충돌, SP 부족 명령을 재현한다.
- 임의 코드, 재귀, 과도한 생성, 즉시 패배, 숨은 규칙을 모두 거부한다.
- 유효·무효·부분 JSON, 타임아웃, HTTP 오류, 중복 요청과 자동 재시도를 검증한다.
- 100개 대표 월드 스냅샷 평가에서 첫 응답 유효율 95% 이상, 수정 재시도 포함 99% 이상을 달성한다.
- 규칙 생성 지연은 목표 NAS 환경에서 p95 8초 이하, 하드 타임아웃 20초다.
- 100개 평가 런 중 80% 이상이 서로 다른 규칙 그래프 서명을 가진다.
- 30턴 동안 규칙 누수, 무한 연쇄, 도달 불가능한 승리 상태가 없어야 한다.
- 저장 중단·손상·스키마 이전·AI 오류 상태에서 정상 복구한다.
- 최신 Chrome과 Edge 데스크톱에서 평균 60 FPS, 최소 30 FPS를 유지한다.
- GitHub Pages 프리뷰와 운영 URL에서 새 게임, 3턴 진행, 저장, 새로고침 복원, AI 규칙 적용을 실제 검증한다.

### 전제와 제외 범위

- 현재 작업 폴더에는 Unity 프로젝트와 Git 저장소가 없으므로 신규 프로젝트로 시작한다.
- 사용자가 라이선스상 웹 배포 가능한 전체 `Assets/Kay`와 GitHub 저장소를 제공한다.
- 시놀로지 NAS에는 Docker, HTTPS 리버스 프록시, 외부에서 접근 가능한 도메인이 준비되어 있다고 가정한다.
- OpenAI 연결은 필수이며 오프라인 플레이와 규칙 폴백은 제공하지 않는다.
- 첫 버전에서는 모바일, 네이티브 PC 빌드, 멀티플레이, 계정, 클라우드 저장, 모드, 런타임 3D 에셋 생성, 임의 코드 실행을 제외한다.
