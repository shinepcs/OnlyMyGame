{
  "description": "Run local API verification when a Codex task stops; production deployment is always manual.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$(git rev-parse --show-toplevel)/scripts/verify-server.sh\"",
            "timeout": 300,
            "statusMessage": "Verifying OnlyMyGame API tests and Release build"
          }
        ]
      }
    ]
  }
}
