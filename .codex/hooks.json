{
  "description": "Deploy the NAS API after Codex finishes a turn that changed API sources.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$(git rev-parse --show-toplevel)/scripts/deploy-nas-api.sh\"",
            "timeout": 180,
            "statusMessage": "Checking whether the NAS API needs deployment"
          }
        ]
      }
    ]
  }
}
